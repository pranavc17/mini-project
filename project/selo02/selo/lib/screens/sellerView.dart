import 'package:flutter/material.dart';

class ScreenSellerView extends StatelessWidget {
  const ScreenSellerView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Text('seller view here'),
    );
  }
}
